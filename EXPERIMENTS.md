# 实验记录（EXPERIMENTS）

所有实验的**设置 / 命令 / 数据 / 结论**。数据集统一是 **MultiHop-RAG**（2556 题；见 README 第三节），已整份传到 LangSmith 数据集 `multihop-rag`（含 gold 答案 + gold 文章标题 + evidence 摘录）。

评测口径统一：
- **LLM-as-judge**（openevals，judge = grok 网关）：`correctness` / `groundedness`(忠实度) / `retrieval_relevance` / `helpfulness`
- **确定性**：`context_recall` = gold 文章标题 ∩ 检索到的 source ÷ gold 标题数；`refused` = 答案是否命中拒答线索词

> ⚠️ 读数须知：LLM-judge **单裁判（grok）、不完全可复现**；小样本波动大。下面看**方向和量级**，别抠绝对值。所有实验对同一套指标口径，横向可比。

---

## 实验 1 · 模型选型：bge-small vs bge-large（n=6）

**问题**：升级到更大的向量/重排模型，指标会更好吗？
**命令**：`python eval_rag.py --n 6`（分别在两组模型下）

| 配置 | correctness | groundedness | retrieval_relevance | helpfulness |
|---|---|---|---|---|
| bge-small-en-v1.5 + reranker-base | 0.67 | 1.00 | 0.72 | 0.67 |
| **bge-large-en-v1.5 + reranker-v2-m3**（现默认，GPU） | 0.49 | 0.98 | 0.65 | 0.48 |

**结论**：n=6 上大模型**没有可辨识的提升**——差异全在 LLM-judge 噪声 + 小样本内（一道题翻转就拉动均值 0.15）。**6 个样本不能给模型选型下结论**（要 n≥20~30）。当前仍默认大模型（benchmark 上更强，且有 GPU）。

---

## 实验 2 · 单次强检索流水线 baseline（`multihop-rag`，n=12）

**问题**：hybrid(BM25+bge+reranker) + 单次生成，在真实数据集上什么水平？
**命令**：`python eval_rag.py --dataset multihop-rag --n 12`
**LangSmith experiment**：`hybrid-on-multihop-rag-be7e9a92`

| correctness | groundedness | retrieval_relevance | helpfulness | context_recall |
|---|---|---|---|---|
| 0.42 | **1.00** | 0.64 | 0.52 | **0.64** |

**结论**：`groundedness` 满分 = **零幻觉**；但 `correctness`(0.42) 被**检索召回**(`context_recall` 0.64)卡住——难多跳证据分散在 2~4 篇，单次只捞到约 2/3，模型**没证据就诚实拒答**（"I don't know"）。瓶颈在检索，不在生成。确定性 `context_recall`(0.64) ≈ LLM-judge `retrieval_relevance`(0.64)，两口径互印证。

---

## 实验 3 · topk 消融（context_recall，纯检索、**免费**不调 LLM）

**问题**：单次检索的召回瓶颈，靠调大 topk 能补多少？
**方法**：30 道多跳（gold 平均 **2.6 篇/题**），只算 context_recall。

| topk | context_recall |
|---|---|
| 4（原默认） | 0.52 |
| 8 | **0.68**（+0.16） |
| 12 | 0.70 |
| 20（= 候选池 pool 顶） | 0.75 |

**结论**：`topk=4` 对"平均要 2.6 篇"的多跳太小 → 元凶。→8 一步 +0.16；8 往上收益骤减，`pool=20` 是天花板。**这只是权宜之计**——见实验 4。

---

## 实验 4 · 单次 vs agentic 多跳（**受控**对比，context_recall）

**问题**：rerank 排的"最相关 top-k"经常漏掉多跳需要的另一篇 gold（它排到 5-8 甚至池外）。多跳改写检索能不能捞回单次**任何 topk 都够不着**的 gold？
**方法**：同 5 道多跳，**同一个 hybrid 检索器**，唯一变量 = 单查询 vs 多跳（把所有 hop 检索 source 取并集）。

| | sp@4 | sp@8 | sp@20（池顶） | **agentic** | 平均跳数 |
|---|---|---|---|---|---|
| 均值 | 0.45 | 0.60 | 0.65 | **0.73** | 4.0 |

- **agentic 0.73 > 单次池顶 0.65**：多跳**越过了单次的天花板**——改写后的 query 把不同片段拉进不同候选池。实锤:Will Smith 那道单次卡 0.50、**多跳 1.00**；Prime 那道单次卡 0.50、**多跳 0.75**（都捞回了池外 gold）。
- **但多跳不免费也不稳赢**：Engadget 那道单次 top-4 本就 1.00，agent 自作主张改写 query → **0.67（更差，query drift）**；Between 那道 **8 跳仍 0.50**（有些精确时序证据改写多少次都够不着）；平均 **4 跳 = ~4× 调用成本**。

**结论**：pointwise rerank 管"相关性"、**管不了多跳的证据覆盖**；多跳用**成本 + 忠实度**换覆盖。

---

## 实验 5 · agentic RAG × 100，按 question_type

**问题**：agentic 多跳在**不同题型**上分别什么表现？
**命令**：`python eval_agentic.py --per-type 25`（comparison / inference / temporal / null_query 各 25 = 100）
**LangSmith experiment**：`agentic-on-multihop-rag-99689380`（0 报错）

| type | correctness | groundedness | retrieval_relevance | helpfulness | **context_recall** | **refused** |
|---|---|---|---|---|---|---|
| comparison (25) | 0.81 | 0.74 | 0.61 | 1.00 | **0.75** | 0.08 |
| inference (25) | **1.00** | 0.71 | 0.86 | 1.00 | 0.58 | 0.00 |
| temporal (25) | 0.58 | 0.73 | 0.53 | 0.96 | 0.69 | 0.24 |
| null (25) | –（无 gold） | 0.95 | 0.03 | 0.35 | – | **0.84** |

- 🟢 **inference 强项**：correctness **1.00**。跨文档推断，agent 多跳凑证据 + 推理最吃得开。
- 🟢 **comparison 稳**：correctness 0.81、context_recall 0.75（四类最高）。
- 🔴 **temporal 最难**：correctness 0.58、retrieval_relevance 0.53、24% 被迫拒答——精确时序 + 跨文档比较，检索与推理双重吃力。
- 🟢 **null 拒答到位**：**84% 正确拒答**（认出"语料里没有"）；剩 16% 硬编 = 残余幻觉风险。

---

## 实验 6 · 混合检索参数调优（**免费**、确定性）+ dense 后端换 Chroma

**问题**：只调**参数**（不引入新算法/模块）、把混合检索推到瓶颈能到多少？顺带把 dense 后端从内存 numpy 余弦换成 **Chroma** 持久化向量库（更规范、可持久化、易扩展；同 `Retriever` 协议，上层一行不动）。
**命令**：`python eval_ablation.py --n 50`（贪心坐标上升 fusion → rrf_k → w_dense → pool）
**口径**：50 道多跳（gold 平均 2.54 篇/题），只算确定性 `context_recall`（**不调 LLM、免费、可复现**）：
- `recall@pool` = 融合后候选池的召回（**重排前的天花板**，纯看融合有没有把 gold 捞进池）
- `recall@4 / @8` = 交叉编码重排后 top-k 真正交给模型的召回

**融合方式：min-max 加权 → RRF（倒数排名融合）**——RRF 只看两路排名、不看分数，对 BM25(无上界) 与余弦([-1,1]) 不可比的量纲免疫（Elasticsearch / Weaviate 标配）。

### 逐阶段（贪心坐标上升，recall@8）

| Stage | 变量 | 结果 |
|---|---|---|
| A 融合 | minmax vs **rrf**（w=.5, pool=20） | 0.747 → **0.757**（@4 0.617→0.640）；rrf 略胜、且量纲无关更稳 |
| B rrf_k | 10 / 30 / 60 / 100（pool=50） | 全部 **0.767** —— 完全不敏感 → 取标准值 **60** |
| C w_dense | 0.2–0.8（pool=50） | 0.762–0.775 噪声级；但 **@4 在 0.3–0.5 最高(0.668)、0.8 掉到 0.647** |
| D **pool** | 20 / 50 / 100 / 150 / 200 | **0.718 → 0.775 → 0.777 → 0.770 → 0.770** |

**pool 是唯一的大杠杆**：20→50 一步 **+0.057**；再往上 `recall@pool` 天花板继续升（0.94→0.98）但**交付 @8 卡死在 ~0.777**。

### Chroma 全栈确认（Chroma dense + RRF + reranker，50 题）

| 配置 | recall@pool | @4 | @8 |
|---|---|---|---|
| minmax w=.5 pool=20（旧默认） | 0.845 | 0.617 | 0.747 |
| **rrf w=.5 pool=100（新默认）** | **0.987** | **0.657** | 0.777 |
| rrf w=.7 pool=100 | 0.975 | 0.640 | 0.783 |

- **Chroma ≈ numpy**：HNSW 在 6194 向量上近似即精确，指标基本不变——换库是**零指标代价**的工程规范化。
- 新默认 vs 旧默认：`recall@4` 0.617→**0.657**、`@8` 0.747→**0.777**、池天花板 0.845→**0.987**。

**结论（参数调优的瓶颈已到）**：
1. **pool（20→100）+ RRF 是全部增益来源**；`rrf_k`、`w_dense` 在合理区间内都不敏感（差异全在 50 题的噪声里）→ **权重保持均衡 0.5/0.5、rrf_k 用标准 60**，不追噪声。agent 工具按 k=4 检索，`w_dense=0.5` 的 @4 最高，故不采纳贪心多给的 0.8。
   > **⚠️ 实验16 归因订正**：结论（保持 0.5/0.5）仍然正确，但**原因不是"两路贡献等价"**。按题型重扫发现 **temporal 上 w_dense 从 0.0 到 1.0 六个取值的 fact@8 完全相同（都是 0.422）**——而纯 BM25 单路是 0.522、纯 dense 单路是 0.344，相差 0.178。**是 reranker 把上游差异整个抹平了**。"不敏感"不是两路等价，是**下游独裁**。
2. **天花板到了**：候选池已覆盖 ~99% 的 gold（`recall@pool`=0.987），但**重排 + 小 top-k 只交付 ~0.66/0.78**——真正的瓶颈是 **reranker 精度 + top-k 太小 + chunk 聚簇**，不是检索覆盖。再往上要动**非参数**手段（更大 k / 按 source 去重·MMR / 更强 reranker / 父文档检索），本轮按约束只调参数，到此为止。
3. **最终默认**：`HybridRetriever(fusion="rrf", w_bm25=0.5, w_dense=0.5, pool=100, rrf_k=60)`；dense 后端 = Chroma 持久化集合（`.cache/chroma/`）。

---

## 实验 7 · agentic RAG × 100 重跑于**调优后**检索栈（RRF + Chroma + pool=100）

**问题**：实验6 把检索层调好了（确定性 `recall@pool` 0.85→0.99），端到端的 agentic 多跳指标会跟着变好吗？
**方法**：同实验5的评测设计与抽样（每类 25 = 100），检索栈整体从 `minmax + pool=20 + numpy` 换成调优后的 **`rrf + pool=100 + Chroma`**。⚠️ 两次为不同批次运行、agent 非严格确定性（网关），端到端差异须放在 run-to-run 噪声（±0.05~0.10 / 25 题）里看。
**命令**：`python eval_agentic.py --per-type 25`　**LangSmith**：`agentic-on-multihop-rag-e7aef6d2`（0 报错）

| type | correctness | groundedness | retrieval_relevance | helpfulness | context_recall | refused |
|---|---|---|---|---|---|---|
| comparison (25) | 0.81 | 0.67 | 0.58 | 1.00 | 0.70 | 0.04 |
| inference (25) | **1.00** | 0.68 | 0.87 | 1.00 | 0.62 | 0.00 |
| temporal (25) | 0.60 | 0.72 | 0.55 | 0.98 | 0.65 | 0.24 |
| null (25) | –（无 gold） | 1.00 | 0.04 | 0.56 | – | **0.96** |

**对比实验5（调优前）——均值 Δ**：

| 指标 | 调优前(实验5) | 调优后(实验7) | Δ |
|---|---|---|---|
| correctness（可答三类均值） | 0.80 | 0.80 | ~0 |
| context_recall（可答三类均值） | 0.67 | 0.66 | ~0（噪声内） |
| groundedness（可答三类均值） | 0.73 | 0.69 | -0.04（噪声内） |
| **null refused** | 0.84 | **0.96** | **+0.12** |
| **null helpfulness** | 0.35 | **0.56** | **+0.21** |

**结论（诚实）**：
1. **检索层的确定性增益（实验6）没有线性传导到端到端**：三类可答题的 correctness / context_recall 基本不变（都在 25 题/类 + 非确定性网关 + LLM-judge 的 run-to-run 噪声内）。
2. **原因**：agentic **多跳本就在补偿单次检索的不足**（实验4 已证多跳能越过单次池顶）——把单次检索器调强，在「已经靠多跳兜底」的端到端上边际收益被摊薄。这是「多跳 vs 强检索是两条互补的路」的又一佐证。
3. **唯一清晰的端到端红利在 null 负例**：`refused` 0.84→**0.96**、`helpfulness` 0.35→0.56——更强的检索让 agent 更容易确认「语料里真没有」从而更干脆地拒答，残余幻觉风险 16%→4%。
4. **净结论**：调优**在检索层是实打实的赢**（实验6，免费可复现），**端到端不亏、拒答更稳**；要在可答题上继续涨 correctness，得靠**非参数**手段（更大 k / 去重·MMR / 更强 reranker / 父文档），而非再拧融合参数。

---

## 实验 8 · source 去重 + k=8 —— 直攻 context_recall（用户点名最重要的指标）

**问题**：实验7 端到端 `context_recall` 卡在 ~0.66。诊断（`eval_diag.py`，免费确定性）确认：**不是覆盖**（top-100 池召回 0.99），是**交付多样性**——每题需 2.54 篇不同文章，但重排后 top-4 只摊到 **2.42 篇**（cross-encoder 把同一篇的多个 chunk 排前面、挤掉别的 gold 文章）。铁证：`recall@4 去重(0.808) > recall@8 不去重(0.777)`——4 个"每篇不同"胜过 8 个"扎堆"。
**修法**：`HybridRetriever.search` 加 **source 去重**（`max_per_source=1`，每篇≤1 个名额）+ agent 工具 k 4→8。
**单次确认**（确定性）：`recall@8` 0.777→**0.887**，top-8 交付 **8.0 篇不同文章**。
**命令**：`python eval_agentic.py --per-type 25`　**LangSmith**：`agentic-on-multihop-rag-9a12125d`（0 报错）

| type | correctness | groundedness | retrieval_relevance | helpfulness | **context_recall** | refused |
|---|---|---|---|---|---|---|
| comparison (25) | 0.68 | 0.74 | 0.60 | 1.00 | **0.85** | 0.08 |
| inference (25) | **1.00** | 0.84 | 0.88 | 1.00 | **0.86** | 0.00 |
| temporal (25) | 0.66 | 0.84 | 0.50 | 0.98 | **0.95** | 0.12 |
| null (25) | – | 0.95 | 0.03 | 0.76 | – | 0.88 |

**对比实验7（去重前）——可答三类均值 Δ**：

| 指标 | 实验7(去重前) | 实验8(去重后) | Δ |
|---|---|---|---|
| **context_recall** | 0.66 | **0.89** | **+0.23** 🟢 |
| groundedness | 0.69 | **0.81** | +0.12 🟢 |
| correctness | 0.80 | 0.78 | ~0（但 comparison 0.81→**0.68**）🟡 |
| retrieval_relevance | 0.57 | 0.55 | ~0 |

**结论（诚实、有反转）**：
1. **召回目标达成**：`context_recall` ~0.66→**0.89**（+0.23）——端到端大涨，且与单次确定性预测一致（多跳并集甚至比单次更吃得到多样性）。
2. **groundedness 跟涨 +0.12**：覆盖更全 → agent 有更多对的证据可依，忠实度更稳。
3. **但 correctness 没跟涨**（0.80→0.78）、**comparison 掉 0.81→0.68**：经典 **recall↑ ≠ correctness↑**——多样性把 8 篇"各不同但部分边缘"的文章塞给模型，在**比较类**（要精确对齐两个具体实体）上引入干扰；`retrieval_relevance` 没涨印证"多样但单篇相关性下降"。（comparison 25 题 + 非确定性 agent，-0.13 有噪声成分，但方向可信。）
4. **净结论**：**要覆盖/召回/忠实度，去重+k=8 是大赢；但正确率是另一层瓶颈（模型会不会用证据），召回单调涨解不了**。折中可选：`max_per_source=2` 或 k=6（留多样性、少干扰），或按题型分策略（comparison 用更小 k / 更高单篇相关性）。

---

## 实验 9 · 撤销去重、只留 k=8 —— 停止刷代理指标后 correctness 反而最高

**问题**：实验8 的去重把 title 级 `context_recall` 刷到 0.89 却让 correctness 掉了（comparison 0.81→0.68）。fact 级诊断（数据集 `evidence_list` 的证据句是否真在检索文本里）实锤——**dedup 刷高 title、却把 fact 级召回从 0.645 打到 0.530**：

| config | title_recall | fact_recall |
|---|---|---|
| nodedup k=8 | 0.770 | **0.645** |
| dedup1 k=8（实验8） | **0.885** | 0.530 |
| pool(100) 天花板 | 0.980 | 0.917 |

去重把每篇里查询分最高、却未必含证据的那个 chunk 留下（拿 title 分），丢掉真正含证据的 chunk（Goodhart：游戏漏水的代理指标）。
**修法**：source 去重**默认关掉**（`max_per_source=None`，仅留作可选项）；保留 k=8——真正的赢是 `k 4→8`（fact_recall 0.487→0.645），与去重无关。
**命令**：`python eval_agentic.py --per-type 25`　**LangSmith**：`agentic-on-multihop-rag-d61482cc`

| type | correctness | groundedness | retrieval_relevance | helpfulness | context_recall | refused |
|---|---|---|---|---|---|---|
| comparison (25) | 0.80 | 0.74 | 0.60 | 1.00 | 0.85 | 0.00 |
| inference (25) | **1.00** | 0.75 | 0.87 | 1.00 | 0.72 | 0.00 |
| temporal (25) | 0.67 | 0.83 | 0.54 | 0.97 | 0.83 | 0.16 |
| null (25) | – | 0.96 | 0.03 | 0.64 | – | 0.92 |

**三次 100 题横比（可答三类均值）**：

| 配置 | correctness | context_recall(title) | groundedness |
|---|---|---|---|
| 实验7 nodedup k=4（基线） | 0.80 | 0.66 | 0.69 |
| 实验8 **dedup1** k=8（刷代理） | 0.78 | **0.89** | 0.81 |
| 实验9 **nodedup k=8**（正解） | **0.82** | 0.80 | 0.77 |

**结论**：
1. **停止刷代理，correctness 反而最高**：实验9 correctness **0.82**（三次最高），comparison 从实验8 的 0.68 **回到 0.80**。真正管用的只是 `k 4→8`（多交付证据）；去重是被 title 级 `context_recall` 带偏的弯路。
2. **`context_recall` 0.80 是「诚实值」**：比实验8 的 0.89 低（不再游戏它）、比基线 0.66 高（k=8 实打实的增益），且现在与 fact/correctness 同向。
3. **方法论教训**：**title 级 `context_recall` 是漏水的代理——一旦当成优化目标就失真（Goodhart）。该盯 fact 级召回 / correctness。**
4. **检索侧近顶**：池里已有 **92% 的证据**（`pool fact_recall=0.917`），交付 ~0.65；再堆检索技巧（去重/MMR/更大 k）边际收益薄且易反噬。后续 correctness 要靠 reranker 把证据 chunk 排进 top-k + 生成端推理。

**评估器订正（端到端实锤）**：给 `eval_common` 新增 fact 级 `context_recall_fact`（gold 证据句到没到检索文本），把实验8/9 的**已存 run** 从 LangSmith 拉回来**免费重打分**（不重跑 agent）：

| run | title_recall（旧 context_recall） | **fact_recall（新）** | correctness |
|---|---|---|---|
| 实验8 dedup1 k=8 | **0.888** | 0.521 | 0.78 |
| 实验9 nodedup k=8 | 0.799 | **0.608** | **0.82** |

**旧指标说实验8更好（0.888>0.799），新指标说实验9更好（0.608>0.521）——两把尺子把配置排反了**；而 fact 级与 correctness 同向。**title 级 `context_recall` 不只是粗，是会给出反向结论的坏评估器；评测已改用 fact 级（`context_recall_fact`）为准，title 级仅留作参考。**

---

## 实验 10 · 切到 fact 级指标 + per-type 8（快版基线）

**改动**（配置不变，仍是实验9 的 rrf+Chroma+pool=100+k=8、不去重）：
1. 评测的召回指标从 title 级 `context_recall` **换成 fact 级 `context_recall_fact`**（gold 证据句是否真在检索文本里）——title 级会把配置排反（实验9），已从默认评测移除、仅留作 `eval_diag` 对照。
2. 默认抽样 `--per-type` 25→**8**（=32 题，~20-40 分钟，省时间）。

**命令**：`python eval_agentic.py`（默认 per-type 8）　**LangSmith**：`agentic-on-multihop-rag-241732c0`

| type | correctness | groundedness | retrieval_relevance | helpfulness | **context_recall_fact** | refused |
|---|---|---|---|---|---|---|
| comparison (8) | 1.00 | 0.79 | 0.72 | 1.00 | 0.65 | 0.00 |
| inference (8) | 1.00 | 0.74 | 0.91 | 1.00 | 0.65 | 0.00 |
| temporal (8) | 0.65 | 0.93 | 0.57 | 0.97 | 0.58 | 0.12 |
| null (8) | – | 1.00 | 0.00 | 0.61 | – | 1.00 |

⚠️ n=8/类，噪声大（一题翻转拉动 0.125），**看方向别抠绝对值**。
**结论**：fact 级召回 ~**0.63**（诚实值，与实验9 重打分的 0.608 一致），不再是 title 级刷出来的 0.80；且与 correctness 同向（temporal 的 fact 0.58 最低、correctness 也最低）。以后迭代就用这套 **fact 级 + per-type 8** 快速看方向。

---

## 实验 11 · 交付瓶颈深挖:fact 级召回卡在哪一段(全免费诊断)

**问题**:fact 级召回交付@8 ~0.65、但候选池覆盖@100=0.92——中间 ~0.27 在哪丢的?能不能修?全部**确定性、免费、不调 LLM**,50 道多跳、fact 级(证据句是否真在检索文本里)。

**① fact_recall 随 top-k**:@4 0.49 / **@8 0.65** / @12 0.71 / **@16 0.75** / @20 0.81 / @100 0.92。
→ 证据大量堆在**第 9-20 名**,`k` 是最便宜的召回杠杆。**已把 agent 工具 `k` 8→16**(交付 0.65→0.75)。

**② 分段单路 fact@8(BM25 / Dense / 融合 / 重排)**:

| 阶段 | fact@8 | 覆盖@100 |
|---|---|---|
| BM25 单路 | **0.583** | 0.883 |
| Dense(bge)单路 | **0.478** | 0.883 |
| 融合(RRF) | 0.565 | 0.917 |
| 重排后 | **0.653** | 0.917 |

→ **向量(bge)是弱腿**(0.478 < BM25 0.583)——新闻+实体库**词法 > 语义**;融合价值在**覆盖**(@100 0.883→0.917);~~**rerank 在帮忙(+0.088)、不是短板**~~;8% 是 BM25+向量**双漏**、任何 reranker 救不了。

> **⛔ 本条已被实验15 推翻**："rerank 在帮忙 +0.088" 是**全题型聚合**，把 comparison/inference 的正增益与 **temporal 的负增益**混在一起抵消了。分题型看：bge 在 temporal 上 **fact@8 −0.100（净减益）**，51% 的证据 chunk 被它**往后推**。详见实验15。

**③ chunk 大小**:1200(0.65)> 600(0.55)> 300(0.38);递归 chunk(尊重 `\n\n`/句子边界)**中性**(0.653 vs 定长 0.645)。→ **1200 已近最优**(切小→块多→池覆盖差)。〔更正:reranker `max_length=8192`,大块不截断,之前"512 上限"是错的。〕

**④ oracle 重排**(query 拼 gold 答案再重排):**无提升**(0.638)——但答案多为 yes/no、信息量低,弱证。

**⑤ 证据被挤出 top-8 的归因(diag3)**:到 top8 **61%** / 同篇导语挤(claim3)**13%** / 别篇半匹配挤(claim1)**16%** / 不在池 10%。**~五五开,无单一银弹**。

**⑥ 换 instruction-aware reranker(Qwen3-Reranker)**:装系统 `python3.14-dev` 解锁 GPU LLM 重排(triton gcc 编译需头文件)。加 instruction"含**任一**证据、哪怕只覆盖一部分"。**0.6B 与 bge 持平**(fact@8 0.652 vs 0.653);但 **Qwen3-4B 大赢**——fact@8 0.653→**0.782**、top8 61%→**75%**、**claim1 16%→7%(腰斩)**。0.6B 太弱看不懂 instruction,4B 够强才兑现"含任一证据"的判别。〔端到端见实验12〕

**结论**:检索交付有**两个真杠杆**——① `k` 8→16(免费,fact@16 0.80);② 换 **instruction-aware reranker Qwen3-4B**(fact@8 0.65→**0.78**、claim1 腰斩)。前者免费、后者慢(4B 每次重排 ~13s)但显著。覆盖仍有 8% 双漏(BM25+向量都没捞到)、任何 reranker 无解。**注意**:这些是"检索交付"的实打实提升;能不能换来 **correctness** 另说(可答题已~满分、temporal 是一致性推理题)——端到端验证见**实验12**。

---

## 实验 12 · 端到端验证 Qwen3-4B —— 顺带挖出一个**把实验读反的评测框架 bug**

**问题**：实验11 的检索交付提升（fact@8 0.65→0.78）能不能换来 correctness？

### ① 两轮跑砸了，原因都不在模型

| 轮次 | 现象 | 真因 |
|---|---|---|
| 第 1 轮 `b49fyzwx4` | correctness 0.14、50 报错 | 网关 **429 Concurrency limit**——4B 每跳重排 ~13s 拉长单题，`max_concurrency=4` 把网关调用叠在一起 → 加 `--concurrency` 参数，降到 1 |
| 第 2 轮 `agentic-qwen-on-multihop-rag-be9525ec` | 0 个 429，却 temporal 0.12 / inference 0.57 | 网关 **Cloudflare 502**（源站抽风，`retry_after: 60`）；而 `target()` 里一句 `except Exception: return 空结果` 把它**吞了** |

**这个 bug 才是重点**：吞掉异常 → 该题产出空答案 → 裁判给 **0 分**并**计入均值**，读起来就是"Qwen3-4B 让模型变差了"。核对存档 run 实锤污染范围：

| type | 8 题里产出为空 |
|---|---|
| temporal | **7** |
| inference | 3 |
| null | 1 |
| comparison | 0 |

**只统计非空题**后重算，结论完全翻转——qwen 的 comparison/inference correctness 都是 **1.00**（与 bge 持平），根本不是"变差"：

| reranker | comparison | inference | temporal | 有效 n |
|---|---|---|---|---|
| bge（实验10） | 1.00 | 1.00 | 0.65 | 8/8/8 |
| qwen4B（第 2 轮，剔空后） | 1.00 | 1.00 | (1.00) | 8/5/**1** |

⚠️ temporal 只剩 1 题有效 → **这轮不足以判定 qwen 的端到端效果**，必须重跑。

**第 3 轮（干净）**：换到另一个可用网关后重跑，**0 报错、32/32 全有效**（LangSmith `agentic-qwen-default-on-multihop-rag-b2f5d752`）：

| type | correctness | groundedness | retrieval_rel | context_recall_fact | refused |
|---|---|---|---|---|---|
| comparison (8) | 1.00 | 0.82 | 0.73 | 0.60 | 0.00 |
| inference (8) | 1.00 | 0.88 | 0.91 | 0.75 | 0.00 |
| temporal (8) | 0.68 | 0.91 | **0.75** | 0.67 | 0.12 |
| null (8) | – | 0.90 | 0.00 | – | 0.88 |

**对比实验10（bge）**：correctness comparison/inference 双双 **1.00 = 1.00**（本就满分）、temporal 0.65→0.68（n=8 噪声内）；
检索侧则实打实地涨：inference fact 0.65→**0.75**、temporal fact 0.58→**0.67**、**temporal 的 `retrieval_relevance` 0.57→0.75**。

> **回答了追了三轮的那个问题：+0.13 的 fact 交付换不来 correctness。** 这不是失败，是把"检索已到头"从推断变成实测——
> 与 ⑤ 的 pool=400 结论完全一致（覆盖补到 100% 也一样不动）。**可答两类早已满分、temporal 卡在推理与查询对齐，检索侧再使劲也没有出口。**

### ② 修法（已落地 `eval_agentic.py`）

1. **绝不吞异常**：网关失败改为**长退避重试**（0/30/90s，抗 Cloudflare 建议的 60s 级抽风；SDK 内置退避只有秒级、扛不住），三次仍失败就 `raise` —— LangSmith 把该题标 **errored 并排除出均值**，而不是当 0 分算。
2. 本地聚合同样**跳过空产出**，并打印 `n有效` + 失败题数告警（`n有效` 远小于抽样数 = 这轮不可信）。

**方法论教训（与实验9 的 Goodhart 教训并列）**：**「容错」写错了地方比不容错更危险**——`except: return 空` 把*基础设施故障*伪装成*模型表现差*，静默地污染均值。评测里失败必须**可见**（errored），不能降级成 0 分。

**⚠️ 后续修正（实验14 期间发现）：「排除 errored」也不是终点。** 在另一网关上以并发 2 跑 bge 对照组时报了 **77 次 RateLimitError、3 题 errored**，`n有效` 只剩 8/6/7/8，而 temporal correctness 显示 **1.00**（明显偏高）。原因：**errored 不是随机的**——多跳越多的题 → LLM 调用越多 → 越容易撞限流 → 越容易被排除；而多跳多的恰恰是**难题**。于是"排除 errored"产生**幸存者偏差、系统性拔高分数**。
> **记 0 分 = 低估，排除 = 高估（当失败与难度相关时）。两种统计处理都救不了，正解是把失败率压到足够低**（降并发/加退避），并**始终盯着 `n有效`**：只要它明显小于抽样数，这轮就不可信，重跑。（该轮已用并发 1 重跑。）

### ③ 免费定位：换 reranker 到底救了哪一类题（`eval_diag.py --by-type`，纯本地 GPU、不依赖网关）

**fact 级召回 × 题型 × top-k**（每类 15 题）：

| type | reranker | @4 | @8 | @16 | pool(100) |
|---|---|---|---|---|---|
| comparison | bge | 0.511 | 0.789 | 0.844 | 0.922 |
| comparison | **qwen4B** | 0.700 | **0.878** | **0.922** | 0.922 |
| inference | bge | 0.528 | 0.633 | 0.878 | 0.933 |
| inference | **qwen4B** | 0.633 | **0.861** | 0.878 | 0.933 |
| **temporal** | bge | **0.367** | **0.422** | **0.622** | 0.867 |
| **temporal** | **qwen4B** | 0.456 | **0.533** | 0.644 | 0.867 |

**Qwen3-4B 对三类都涨**（@8：comparison +0.089、inference **+0.228**、temporal +0.111）。更关键的是**它把前两类榨到了池子的极限**——comparison 的 @16 **等于**池天花板（0.922 = 0.922）、inference 的 @8 也逼近（0.861 vs 0.933）：对这两类，**reranker 已经不是瓶颈了，池子才是**。

**唯独 temporal 是"池里有、却送不上去"**：qwen 也只交付 0.533/@8、0.644/@16，池里却有 0.867——最强的 reranker 仍漏掉近 1/4。这与端到端 correctness 的排序完全一致（temporal 0.65，另两类 1.00），**fact 级指标又一次与 correctness 同向**（title 级做不到）。

**排除一个显而易见的解释：temporal 并不是"要凑的证据更多"**（数据集统计，全量）：

| type | gold 文章/题 | 证据句/题 | 证据句均长 |
|---|---|---|---|
| inference | **3.20** | 3.36 | 169 |
| temporal | 2.41 | 2.45 | 163 |
| comparison | 2.21 | 2.23 | 162 |

**inference 要凑的文章最多（3.20）、fact@16 却最高（0.878）；temporal 要得更少（2.41）、召回反而最低。**
→ 所以 temporal 的难不在"证据量"，在**问法与证据的词面/语义不对齐**：它的问句是元层面的一致性追问（"X 在 A 日的报道，与 Y 在 B 日的报道是否一致？"），而 gold 证据句是**具体事实陈述**，两者几乎不共享词汇——BM25 无从匹配、向量也只匹配到"报道/日期"这类表层。

### ④ 推翻实验6 的"pool=100 已是天花板"——那是在**错的口径**上量的

顺着"temporal 池子最薄"往下探（只用融合层、不加载 reranker，故免费且不占 GPU）：

| type | pool=100 | pool=200 | pool=400 |
|---|---|---|---|
| comparison | 0.922 | 0.922 | **0.978** |
| inference | 0.933 | **1.000** | 1.000 |
| **temporal** | **0.867** | 0.978 | **1.000** |

**pool 100→400 把 temporal 的覆盖从 0.867 顶到 1.000——证据全都在池子里了。**

实验6 当时的结论是"pool 超过 100 无收益"，为什么反了？因为那次量的是 ① **title 级**代理指标、② **重排后交付的 @8**。两个口径都会掩盖池子的真实增益：加大池子只是把证据**放进候选**，能不能**送到 top-8** 取决于 reranker——而当时用的是 bge，它本来就排不动这些半匹配证据（实验11 claim1 16%）。**"更大的池子"和"更强的 reranker"是互补的：池子决定上限，reranker 决定兑现多少。** 单独调任一个都会得出"没用"的结论。

→ 直接推论：**pool=400 + Qwen3-4B 才是该测的组合**（弱 reranker 配大池子只会引入噪声）。见 ⑤——**这个推论被自己的实验推翻了。**

### ⑤ 决定性实验：pool=400 × Qwen3-4B —— **覆盖补满了，交付纹丝不动**

同一批题（每类 15）、同一个 4B reranker，只把池子 100→400（重排代价也 ×4，~13s→~52s/题）：

| type | @8 (pool=100) | @8 (pool=400) | @16 (pool=100) | @16 (pool=400) | 池覆盖 100→400 |
|---|---|---|---|---|---|
| comparison | **0.878** | 0.833 | 0.922 | 0.922 | 0.922 → 0.978 |
| inference | 0.861 | 0.878 | 0.878 | 0.894 | 0.933 → **1.000** |
| **temporal** | 0.533 | **0.533** | 0.644 | 0.689 | 0.867 → **1.000** |

**读数：池子里 temporal 的证据从 86.7% 补到 100%，交付@8 一动不动（0.533 → 0.533）。** comparison 甚至掉了 0.045（多出来的候选是噪声）。@16 最多 +0.045。**4× 的算力换来 ~0。**

**所以 ④ 的"池子与 reranker 互补"是错的**，被这个实验证伪。真相更硬：

> **temporal 的证据"排不出来"不是因为不在池子里，也不是因为 reranker 不够强——而是因为它与问句之间根本没有可排序的信号。**

reranker 只能看到 (question, doc)。temporal 的问句是元层面追问（"A 在 X 日的报道与 B 在 Y 日的报道是否一致？"），gold 证据是**具体事实陈述**；两者不共享词汇，也不共享语义焦点。**无论候选池多大、打分器多强，从这个问句出发都无法把那条证据排到前面。** 这也解释了为什么 comparison/inference 已经贴着池天花板（它们的问句直接点名了实体）——那两类的瓶颈确实只剩池子，而 temporal 差的是**问法本身**。

**可行的方向只剩查询侧**（不在本轮范围）：把 temporal 问句**分解**成对每个报道/日期的独立子查询再各自检索（agent 多跳已在部分做这件事，见实验4），或抽出实体+日期做结构化过滤。**继续在"更大池子/更强 reranker"上投入是无效的——本实验已经把这条路走到头并证伪。**

### 净结论（实验12）

1. **Qwen3-4B 是实打实的检索交付提升**（@8：inference +0.228、comparison +0.089、temporal +0.111），且对 comparison/inference **已把池子榨干**（@16 = 池天花板）。代价：~13s/次重排、~8GB 显存 → **评测级配置，不适合实时**。
   > **⚠️ 实验15 归因订正**：comparison/inference 的提升成立且更大（相对**不重排**分别 +0.244 / +0.333）；但 **temporal 的 +0.111 是"止损"不是增益**——bge 在该题型上净减益 −0.100，4B 只是把它修回约 0，天花板仍是不重排的 0.522。
2. **加大池子（100→400）没有价值**：覆盖补满，交付不变，算力 ×4。**pool=100 保持默认**（实验6 的结论侥幸正确，但当初的论证是错的——见 ④）。
3. **检索这条路已经到头了**：可答的两类贴着天花板，剩下的 temporal 是**查询-证据对不齐**，属于查询理解/分解问题，不是检索排序问题。
4. **评测框架的 bug 比模型结论更值钱**（见 ①②）：`except: return 空` 把网关故障伪装成模型退步，静默污染均值。**评测里失败必须可见。**

---

## 实验 13 · 换答题模型：grok-4.5 vs deepseek-v4-pro —— 顺带被迫解决「**裁判模型下线**」

**触发**：网关某天起**不再提供 grok-4.5**（`404 model_not_found: not supported by any configured account`，`/v1/models` 只剩 14 个 deepseek）。而**实验1-12 的分数全是 grok 判的**——基线的尺子没了。

### ① 三个必须先修的评测基础设施问题

1. **答题模型与裁判模型共用同一个 `RAG_MODEL`**：直接换模型 = **同时换掉被测对象和尺子**，分数变化归因不了。
   → `build_model(model=...)` 加显式入参、`build_agent(retr, model)` 透传、`eval_agentic.py --model`；**答题端换、裁判端不动**。
2. **deepseek 全系当不了 openevals 裁判**：四轴**全部**解析失败、只剩确定性指标（一整轮 LLM-judge 静默变 `-`）。
   原因：openevals 内部调 `judge.with_structured_output(schema)`，ChatOpenAI 默认走 `response_format: json_schema`，**本网关不支持** → 模型改用**散文**下结论（"该回答正确。依据…"），解析器全崩。
   → 新增 `llm.build_judge()`：**强制 `method="function_calling"`**（同一网关上 function calling 是好的——agent 的 `rag_search` 就靠它）。修好后重打 64 题 **0 失败**。
3. **裁判换了怎么和旧实验比？** 新增 **`eval_rescore.py`**：run 的 answer/contexts 都存在 LangSmith，**不重跑 agent、只用新裁判重打分**，把新旧实验拉到**同一把尺子**上。

### ② 确定性对比（不依赖裁判，直接可比）：**deepseek 几乎不多跳**

| type | 平均检索次数 grok / deepseek | fact 召回 grok / deepseek | 答案均长 grok / deepseek |
|---|---|---|---|
| comparison | 2.12 / **1.38** | 0.65 / 0.58 | 1537 / 627 |
| inference | 2.00 / **1.12** | 0.65 / 0.68 | 1165 / 651 |
| **temporal** | **3.38 / 1.12** | **0.58 / 0.33** | 2420 / 1041 |
| null | 3.38 / **1.50** | refused **1.00 / 0.75** | 239 / 163 |

**一个行为差异解释了全部**：deepseek-v4-pro 把系统提示里的 **STOP（"够了就停"）执行得很彻底、ITERATE（"多跳改写"）却基本没执行**，平均只搜 1.1~1.5 次。于是——
- **temporal 掉最狠**（fact 0.58→0.33）：正是 grok 跳得最多（3.38）的题型；
- **null 拒答 1.00→0.75**：grok 要搜 3.4 次才敢确认"语料里真没有"，只搜 1.5 次就更容易硬答；
- 答案长度**减半**。

⚠️ `refused` 在可答题上有**假阳性**：像"是否有文章暗示投注者**没有**选项？"这种**正确答案本身就是否定句**的题，答案里自然出现"并未找到 / not contain"，子串匹配会误判成拒答（中英文皆然）。故可答题的 refused 只作参考，别当漏答率读。

### ③ 同一把尺子下的 LLM-judge（裁判 = `deepseek-v4-pro-nothinking`，两轮都重打，各 32 题 0 失败）

| type | correctness grok / deepseek | groundedness grok / deepseek |
|---|---|---|
| comparison | **1.00** / 0.81 | 0.88 / **0.94** |
| inference | **0.99** / 0.93 | 0.62 / **0.80** |
| temporal | 0.75 / 0.75 | **0.84** / 0.55 |
| null | – / – | **1.00** / 0.75 |
| **可答三类均值** | **0.91** / 0.83 | 0.78 / 0.76 |

**结论**：
1. **grok-4.5 的 correctness 仍领先（0.91 vs 0.83）**，差距全在 comparison（1.00→0.81）与 inference（0.99→0.93）；**temporal 打平 0.75**——它卡在推理/查询对齐，换答题模型也救不了（与实验12 一致）。
2. **deepseek 的短板不是"答得差"，是"查得少"**：它在**已有证据**上的 groundedness 反而更高（comparison 0.94、inference 0.80），但只跳 1 次导致证据不全 → correctness 落后、null 更容易硬答。**agentic RAG 的收益依赖模型真的愿意多跳**：策略写在提示里，**执行与否是模型属性**（实验4 已证多跳才是越过单次天花板的关键）。
3. **改进方向**（未做）：对 deepseek 需要更硬的多跳约束（最少检索次数 / 把 ITERATE 变成硬性检查），而不是依赖模型自觉。
4. **提示词语言会外溢**：系统提示是"英文指令 + 中文轴标注"，grok 用英文答、**deepseek 整段用中文答**，导致只认英文线索词的 `refused` 失效 → `REFUSAL` 已改成**中英双语**。**又一次印证实验12 的教训：评估器自身的盲区会伪装成模型表现差。**

### ④ 换裁判后旧结论还成立吗

用新裁判重打**实验10 的同一批 run**：correctness comparison 1.00（原 1.00）、inference 0.99（原 1.00）、temporal **0.75**（原 0.65）。**两把尺子基本一致**（temporal 差 0.10，在小样本 + 裁判噪声内）→ 实验1-12 的**定性结论不因换裁判而翻盘**；但**绝对值以重打后的为准**。

---

## 实验 14 · 把变量换成**问句本身**：查询分解 > 换更强的 reranker

> ⚠️ **"唯一越过天花板"的措辞已失去依据**（原文保留不动，见实验20 ⑤）：本实验的对照基线"不重排 = 0.522"来自实验15 的同一批 **15 题**，而那批数据的 temporal 效应已证明落在噪声内。**查询分解本身可能仍然有效，但需要 n≥60 + 置信区间重测才能下结论**（尚未做）。

**动机**：实验11-12 把检索侧走到了头——池覆盖能补到 100%、换最强的 instruction-aware reranker（Qwen3-4B）也只把 temporal 交付从 0.42 抬到 0.53。根因是 reranker 无论多强都只是 `(query, doc) → 分数`：**query 里没有的信号，打分器变不出来**。temporal 问句是**元层面追问**（"A 在 X 日的报道与 B 在 Y 日的报道是否一致？"），gold 证据是**具体事实陈述**，两者不共享词汇也不共享语义焦点。
**方法**（`eval_query.py`，确定性、只花几十次 LLM 调用生成子问句）：检索器 / reranker **全程不变（bge）**，唯一变量 = **用什么 query 去查**。

**⚠️ 层次澄清（用户指出，之前写错过）**：MultiQuery 作用在**工具内部**（一次 `rag_search` 内把问句扩成 N 个变体、并行检索合并，对 agent 仍是**一次**工具调用）；agentic 多跳作用在**工具之上**（agent 决定调几次、看到结果再定下一跳）。两者**正交、可叠加**，收益相乘，不是二选一。

### temporal（15 题，fact 级）

| 策略 | @4 | @8 | @16 | 池覆盖 |
|---|---|---|---|---|
| 1 baseline（原问句） | 0.367 | 0.422 | 0.622 | 0.867 |
| 2 **multiquery**（LLM 同义改写 ×3 → RRF 合并 → **原问句**重排） | 0.367 | 0.422 | 0.622 | **0.978** |
| 3 decompose（分解成事实性子问句 ×3 → RRF 合并 → **原问句**重排） | 0.367 | 0.422 | 0.622 | 0.956 |
| 4 **decompose-each**（同上分解，但**每个子问句各自重排**再拼） | **0.489** | **0.578** | **0.667** | 0.956 |

**读数（关键）**：
1. **策略 2/3 的三个交付数字与 baseline 完全相同**——不是"提升小"，是**一点没动**；而池覆盖明明涨到 0.978/0.956。**证据被召回进了池子，又被重排原样扔出去。**
2. 原因：2/3 最后都拿**原问句**重排。候选池换了、打分函数没换，它照样按元层面措辞打分，选出来还是那 8 个。**扩大召回的收益 100% 被重排吃掉。**
3. **只有让每个子问句各自重排（策略4）才兑现**：fact@8 **0.422 → 0.578（+0.156）**。

### 与"换 reranker"的性价比对比（同为 temporal fact@8）

| 手段 | fact@8 | 成本 |
|---|---|---|
| 换 Qwen3-Reranker-4B（实验11-12） | 0.422 → 0.533（+0.111） | ~13s/次重排、~8GB 显存 |
| **查询分解 + 各自重排**（本实验） | 0.422 → **0.578（+0.156）** | **1 次 LLM 调用**，仍用便宜的 bge |

**分解比换 4B reranker 效果更好、成本更低。**

**结论**：
1. **瓶颈确实在 query 侧**（实验12 的推断被证实）；但 **MultiQueryRetriever 这个具体做法不够**——它只改召回、不改重排，而瓶颈恰在重排。**同义改写还会把"元层面"这个病一起保留**（实测改写产物仍是"Did the character of reported events shift after…"，仍不落到事实层）。
2. **必须"分解到事实层 + 每个子问句各自重排"**，两个条件缺一不可。
3. **落地位置是 `tools.py` 的 `rag_search` 内部**，不是 agent 层：对外仍是一次工具调用 → `prompts.py`/`agent.py` 一行不动（实验5-13 的基线不用重跑），单次流水线（`run.py`/`eval_rag.py`）同样受益，且与多跳叠加。
4. **额外价值**：这对"模型不肯多跳"是**不依赖模型自觉**的解法——实验13 里 deepseek 只跳 1.12 次且提示词逼不动它，但可以让它那唯一一跳的召回接近多跳水平。

---

## 实验 15 · reranker 是增益还是**减益**？——分题型 + 看证据 chunk 的名次变化

> ⚠️ **本实验的头条数字 −0.100 没能复现，已被实验20 ② 推翻**（原文保留不动）：同一配置把每型 15 题加到 60 题（原 15 题是新样本的子集），temporal Δ@8 变成 **+0.031，95% CI [−0.042,+0.108]（不含 −0.100）**；"51% 被推后、中位名次 6→9"也变成 39% 被推后、8→7 **推前**。
> **原因**：15 题的标准误差约 0.09，而结论依据的效应量是 0.10——信噪比约等于 1。**"分题型看"这个动作是对的，但拆子群会让样本变小、更容易把噪声读成信号；分子群分析必须配置信区间。**
> 受牵连的还有 ③ 那张表（"temporal 天花板 = 不重排 0.522"）及基于它的实验14 结论，详见实验20 ⑤。

**动机**（用户提出）："召回率还是很低，表象是相关线索都被排到后面——reranker 会不会在帮倒忙？"
之前只测过**全题型聚合**的"重排后 vs 融合后"（实验11 ②，+0.088），从没**分题型**看，也从没直接看**含 gold 证据的 chunk 的名次**在重排前后怎么变。
**方法**（`eval_rerank_effect.py`，确定性、免费）：每类 15 题，对比"融合后直接取 top-k"（reranker 完全不介入）与"重排后取 top-k"；并找出候选池里**真含 gold 证据句**的 chunk，记录它在两个列表里的名次。

### ① fact 级召回：融合(不重排) vs 重排

| type | reranker | 融合@8 | 重排@8 | Δ@8 | Δ@16 |
|---|---|---|---|---|---|
| **temporal** | bge | 0.522 | 0.422 | **−0.100** 🔴 | −0.044 |
| **temporal** | qwen4B | 0.522 | 0.533 | +0.011 | −0.022 |
| comparison | bge | 0.633 | 0.789 | +0.156 | +0.122 |
| comparison | qwen4B | 0.633 | **0.878** | **+0.244** | +0.200 |
| inference | bge | 0.528 | 0.633 | +0.106 | +0.300 |
| inference | qwen4B | 0.528 | **0.861** | **+0.333** | +0.300 |

### ② 含 gold 证据的 chunk：被推前还是推后（名次 0 起）

| type | reranker | 融合中位名次 | 重排中位名次 | 被推后 | 被推前 |
|---|---|---|---|---|---|
| **temporal** | bge | 6.0 | **9.0** | **51%** | 26% |
| **temporal** | qwen4B | 6.0 | 5.0 | 37% | 46% |
| comparison | bge | 4.0 | 2.5 | 23% | 47% |
| comparison | qwen4B | 4.0 | **2.0** | 20% | **60%** |
| inference | bge | 7.0 | 3.0 | 21% | 60% |
| inference | qwen4B | 7.0 | **3.0** | 16% | **65%** |

**机制**：temporal 的证据 chunk 被 bge **51% 往后推**、中位名次从第 6 推到第 9——**正好跨过 k=8 的门槛**，这就是 fact@8 掉 0.100 的直接原因。comparison/inference 恰好相反（第 4/第 7 → 第 2.5/第 3）。

**结论（三段，不能合成一句）**：
1. **bge 在 temporal 上是净减益**（−0.100）——**"reranker 在帮忙"是聚合造成的错觉**，实验11 ② 那条已作废。
2. **Qwen3-4B 在 temporal 上只是"止损"、不是增益**（+0.011@8、−0.022@16）：它把 bge 的 −0.100 修回约 0，天花板仍是"什么都不做"的 **0.522**。
   → **实验11-12 说"Qwen3-4B 把 temporal fact@8 从 0.422 提到 0.533"数字没错、归因错了**：它不是"更会排证据"，而是**停止把证据往后压**。这也解释了实验12 为什么"交付涨了 correctness 不动"——temporal 的交付**从未真正超过不重排的水平**。
3. **但 Qwen3-4B 在 comparison/inference 上是货真价实的大增益**（+0.244 / +0.333，远超 bge 的 +0.156 / +0.106）。**不能因为 temporal 的结论就否定它。**

### ③ 把四种做法放在同一把尺子上（temporal fact@8）

| 做法 | fact@8 | 相对"不重排"的净值 |
|---|---|---|
| **分解 + 各自重排**（实验14 策略4） | **0.578** | **+0.056** ✅ 唯一真正突破天花板 |
| 不重排（纯 RRF 融合顺序） | 0.522 | 0（基线） |
| Qwen3-4B 重排 | 0.533 | +0.011（≈ 止损到零） |
| bge 重排（现默认） | 0.422 | **−0.100**（负资产） |

**"不重排"一直被当成下限，其实它是 temporal 上的天花板。** 唯一越过它的是**查询分解 + 各自重排**——它不是绕开 reranker，而是**把 reranker 的输入换成它擅长的那种问句**，让它重新做正功。

---

## 实验 16 · 检索栈**分层**拆解：BM25 / Dense / 融合 / 重排，各自到底贡献了什么

> ⚠️ **本实验的「融合无用 / 上游空间≈0」已被实验19 ④ 推翻**（原文保留不动）：这里的测量是在 **chunk=1200 的坏 dense 腿**上做的。把 chunk 改成 600 后，融合相对更好单路腿的净增益从 @4 的 **−0.026 变成 +0.022**、@16 从 +0.013 变成 **+0.059**。**当时测得没错，错在把"坏输入下的表现"当成了部件的固有属性。**

**动机**（用户问）："有没有只 BM25、只 embedding、两者融合、融合+reranker 这四种的数据？"
有——实验11 ② 测过，但那是 **50 题全类型聚合**，而实验15 刚证明聚合会把子群的负增益藏起来。故按题型重测（每类 15 题，fact 级，`eval_rerank_effect.py`）。

### ① 四层 × 三题型

| type | ① BM25 单路 | ② Dense 单路 | ③ 融合(RRF) | ④ 融合+重排 |
|---|---|---|---|---|
| **temporal** @8 | **0.522** | 0.344 | 0.522 | **0.422** 🔴 |
| temporal 覆盖 | **0.889** | 0.889 | **0.867** 🔴 | 0.867 |
| comparison @8 | 0.667 | 0.556 | 0.633 | **0.789** |
| comparison 覆盖 | 0.878 | 0.833 | **0.922** | 0.922 |
| inference @8 | 0.544 | 0.472 | 0.528 | **0.633** |
| inference 覆盖 | 0.844 | 0.889 | **0.933** | 0.933 |

1. **融合从未提升交付**（0.522=0.522、0.633<0.667、0.528<0.544）——三类里要么持平、要么**低于 BM25 单路**。它的价值**纯粹在覆盖**（0.878→0.922、0.844→0.933）。
2. **temporal 上融合连覆盖都是负的**（0.889→0.867）：dense 在该题型弱到只有 BM25 的 2/3（0.344 vs 0.522），融合把这些劣质候选混进 pool=100，**挤掉了 BM25 本来捞到的好结果**——用弱腿换强腿。
3. **temporal 的最优配置是「BM25 单路、什么都不做」**（0.522 / 覆盖 0.889），比整条 pipeline（0.422 / 0.867）都好。**融合 −0.022 覆盖、重排再 −0.100 交付，整条链路对该题型是纯负贡献。**

### ② 混合排序（不让 reranker 独占 top-k）：是**交换**，不是改进

| 策略 | temporal | comparison | inference | 三类均值 |
|---|---|---|---|---|
| ④ 纯重排 | 0.422 | **0.789** | 0.633 | 0.615 |
| ⑤ 混合 RRF(重排,融合) | 0.500 | 0.689 | 0.522 | 0.570 |
| ⑥ 混合交替取 | 0.467 | 0.756 | **0.650** | **0.624** |

⑥ 在 temporal 上挽回 +0.045、却在 comparison 上赔掉 −0.033，均值优势（0.624 vs 0.615）完全在噪声内。**它把伤害摊薄了、没有消除。**

### ③ 按题型扫 `w_dense`：**六个取值，temporal 的交付一模一样**

| w_dense | 0.0（纯BM25） | 0.2 | 0.3 | 0.5 | 0.7 | 1.0（纯dense） |
|---|---|---|---|---|---|---|
| **temporal @8** | **0.422** | **0.422** | **0.422** | **0.422** | **0.422** | **0.422** |
| comparison @8 | 0.789 | 0.789 | 0.789 | 0.789 | 0.756 | 0.722 |
| inference @8 | 0.633 | 0.633 | 0.600 | 0.633 | 0.611 | 0.578 |

**纯 BM25 单路 0.522、纯 dense 单路 0.344，相差 0.178；经过 reranker 后两者都变成 0.422。**
→ **实验6 的"权重不敏感"结论对、归因错**：不是两路等价，是**reranker 把上游差异整个抹平**（已在实验6 就地订正）。

### ④ 统一图景：上游优化空间 ≈ 0，reranker 是唯一决定者

```
BM25(强腿) ─┐
            ├─ 融合 ─→ pool（覆盖 0.87~0.93，接近饱和）─→ reranker ─→ top-k
Dense(弱腿) ─┘         ↑                                      ↑
                 权重/融合方式/pool 大小              ← 唯一真正决定交付的一环
                 只要不改覆盖，都会被下游抹平            且在 temporal 上做负功
```

**这解释了为什么之前所有努力都不动**：pool 100→400（实验12）、换 Qwen3-4B（实验11-12）、调权重（实验6）、混合排序（本实验②）——全都在 reranker 的**上游或平行位置**使劲，而那里的空间早被封死。
**唯一有效的是改变 reranker 的输入**——即实验14 的查询分解（temporal 0.578，唯一越过"不重排"天花板 0.522 的做法）。

### ⑤ 方法论：三次"数字对、机制错"

| 结论 | 数字 | 当初的解释 | 真实机制 |
|---|---|---|---|
| 实验6「权重不敏感」 | 对 | 两路贡献等价 | **被 reranker 抹平** |
| 实验11「rerank 在帮忙 +0.088」 | 对（聚合） | 重排不是短板 | **temporal 上 −0.100** |
| 实验12「Qwen3-4B 是提升」 | 对 | 更会排证据 | **只是止损到零** |

**三次都是数字没错、机制解释错了。而机制错了，下一轮投入就会砸在错的地方。**
→ **教训：拿到一个数字后，必须能说出"如果我的机制解释是对的，那么 X 实验应该出现 Y 结果"，并真的去测 X。** 光看数字方向对就往下走，会连错三轮。

---

## 实验 17 · 全链路排查：**embedding 在最简单的任务上只有 45% 命中率**

**动机**（用户："embedding 也很有问题啊，排查一下全链路"）。把链路拆成四层，逐层问"这一层的上限是多少"，第一个塌下来的就是元凶（`eval_chain.py`，确定性、免费）。

### ① L0 数据层：证据句在不在语料里 → **100% 都在**

| type | 证据句数 | 在语料里 |
|---|---|---|
| temporal | 41 | **100%** |
| comparison | 33 | **100%** |
| inference | 49 | **100%** |

切分没切断任何证据句。**数据层无罪，排除。**

### ② L1 检索器自检：**用证据句原文当 query**（最有利的输入，理想 top1≈100%）

| 检索器 | temporal | comparison | inference |
|---|---|---|---|
| BM25 | **100%** | **94%** | **100%** |
| **dense** | **63%** | **61%** | **53%** |

**这是硬伤。** 用一句话的**原文**去检索包含这句原文的 chunk——检索里最简单的任务——向量只有 53~63% 排到第 1，BM25 接近满分。
（L3 用真实问句时 dense 掉到 33~40%，那是已知的语义鸿沟，与本条是两回事。）

### ③ 定位：不是 Chroma，是 **chunk 太长**

先排除索引：用缓存向量做**精确余弦** vs Chroma 的 HNSW——

| | top1 命中 | 两者选出同一 chunk |
|---|---|---|
| 精确余弦 | 45% | **100%** |
| Chroma(HNSW) | 45% | |

**HNSW 无罪**（100% 一致）。→ 问题在**编码本身**。变 chunk 大小重测（唯一变量）：

| chunk 大小 | 片段数 | **dense 自查 top1** | BM25 自查 top1（对照组） |
|---|---|---|---|
| **1200（现默认）** | 6,711 | **45%** | 98% |
| 600 | 14,766 | **61%** | 98% |
| 300 | 32,040 | **79%** | 100% |

**dense 单调改善 45%→79%（+34 点），BM25 全程不动。** 根因确认：**1200 字符的 chunk 稀释了句子级语义**——一条 ~160 字符的证据句只占 13%，bge 输出的是整块的"平均语义"，那 13% 被其余 87% 淹没。BM25 是词频匹配，不受块长影响。

> ⚠️ 与实验11 ③ 不矛盾：那里测的是**重排后的整体 fact@8**（1200 > 600 > 300），小块整体更差是因为每块信息量少、top-8 装不下足够证据。**"dense 检索精度"与"交付信息量"是两个相反方向的指标，此前从未分开测过。**

### ④ 对症解法：small-to-big（小块建索引检索 → 映射回父块交付）

嵌套切分（父 1200 / 子 300，映射显式），融合后**未重排**的 fact 召回：

| type | 方案 | @4 | @8 | 覆盖@100 |
|---|---|---|---|---|
| temporal | 现方案（父块直检） | 0.356 | **0.522** | **0.889** |
| temporal | small-to-big | **0.422** | 0.467 | 0.867 |
| comparison | 现方案 | 0.411 | 0.633 | 0.922 |
| comparison | small-to-big | **0.444** | **0.656** | **0.944** |
| inference | 现方案 | 0.417 | 0.528 | **0.933** |
| inference | small-to-big | **0.456** | **0.600** | 0.894 |

**@4 三类全胜（均值 +0.046）**——精度提升如期兑现；但 **@8 仅 +0.013**、temporal 反而 −0.055（小块检索让前排更准，去重映射回父块后候选更集中、覆盖略降）。
→ **是真实但温和的改进，不是根治**；它修的是①，修不了②③。

### ⑤ 全链路结论：三层各有各的病，互相独立

| 层 | 症状 | 证据 | 修法 | 效果 |
|---|---|---|---|---|
| **① dense 编码** | chunk 1200 稀释语义，自查 top1 仅 **45%** | chunk 300→79%；BM25 全程 98~100% | small-to-big | @4 +0.046（温和） |
| **② reranker** | temporal 上净减益 **−0.100** | 51% 证据被推后（实验15） | 查询分解 | temporal 0.422→**0.578** |
| **③ query** | 问句与证据抽象层级错配 | L1 63% → L3 33%（实验14/17） | 查询分解（同上） | 同上 |

**BM25 是整条链路唯一健康的部件**（自查 98~100%，真实问句下也全面优于 dense）。

**最该记住的一条**：之前所有调优（pool、权重、融合方式、换 Qwen3-4B）都建立在一个**编码质量只有一半水准**的底座上，而这个硬伤**从项目第一天就存在、被所有指标漏掉了**——因为 `fact@8` 是端到端指标，看不见"dense 自查 top1"这种**单点能力**。
> **教训：端到端指标必须配一组"单点能力自检"（给每个部件最有利的输入，看它能不能做到接近满分）。否则某个部件长期半残，整体指标只会显示"就这样了"，不会告诉你是谁拖的。**

---

## 实验 18 · 重做实验11 的 chunk 对比：**当年的对比不公平，最优是 ~500 而非 1200**

> ⚠️ **标题里的"~500"已被实验19 ① 修正为 600**（原文保留不动）：本实验全部 `fact@k` 用的是**「证据句前 120 字符」**口径，**看不见证据被切断**——实测 chunk=400 时全句留存只剩 89~95%，而"前120留存"仍报 100%。**这把尺子对 chunk 大小不中立，系统性地偏袒小块。** 方向（小块胜大块）成立，落点应为 **600/overlap150**。另：本实验 ④ 报 chunk=1200 的证据句留存 100%，实验19 在按题型均衡的取样上测得 temporal 是 98.7%——取样不同，**留存并非随块变大而单调**。

**动机**（用户："chunk 小一点试试，也就是重做实验11"；随后追问："我们现在还是按固定字符量 chunk 而不是递归/根据结构吗？"）

### ① 实验11 ③ 的对比是**不公平**的

当年结论"1200(0.65) > 600(0.55) > 300(0.38)"用的是**固定 `fact@8`**——可是 8 个 1200 字符的块 = 9600 字符，8 个 300 字符的块只有 2400 字符，**信息量差 4 倍**，小块必输。
**正确做法是按「总字符数」对齐**（`eval_chain.py --chunk-sweep`）：

| type | 1200@8（9600字符） | **600@16（9600字符）** | 300@32（9600字符） |
|---|---|---|---|
| temporal 融合/重排 | 0.522 / 0.422 | **0.644 / 0.533** | 0.500 / 0.511 |
| comparison 融合/重排 | 0.633 / 0.789 | **0.722 / 0.789** | 0.633 / 0.789 |
| inference 融合/重排 | **0.528 / 0.633** | 0.478 / 0.611 | 0.478 / 0.572 |
| **三类均值** | 0.561 / 0.615 | **0.615 / 0.644** | 0.537 / 0.624 |

4800 字符档差距更大：600@8 对 1200@4，temporal **+0.200 融合 / +0.122 重排**、comparison **+0.211 / +0.222**。
→ **600 全面优于 1200**（仅 inference 略输 0.02~0.05）；300 又掉回去——存在**甜点**：够小以保住 dense 精度、够大以保住信息密度。

### ② 现在到底是不是"按结构切"？——是递归，但被 `chunk_size` 架空了

语料**有结构**：609/609 篇正文都含 `\n\n`，中位 **33 段**，正文均长 10340 字符（**平均每段 ~310 字符**）。
但 `chunk_size=1200` 要塞进约 4 段，塞不下就沿 separators 一路退到按空格切：

| 策略 | 块数 | 中位长 | 贴上限 | 非句尾 | **词中断** |
|---|---|---|---|---|---|
| 递归 1200（现状） | 6,711 | 1032 | 17% | 32% | 28% |
| 递归 600 | 14,766 | 464 | 11% | 36% | **32%** |
| 递归 300 | 32,040 | 208 | 10% | 52% | **49%** |
| **纯段落（上限600）** | 12,954 | 487 | 20% | **27%** | **24%** |
| 纯段落（上限1000） | 7,752 | 846 | 18% | 30% | 26% |

→ 现状 **41% 的块贴着 1200 上限、27% 断在词中间**：**名义递归、实质接近定长**。
→ ⚠️ 但**"块越小结构越完整"是错的**：递归600 的词中断（32%）比 1200（28%）还高，300 更是 49%。

### ③ 决定性对比：**dense 精度只跟"块长"有关，跟"切分方式"无关**

| 配置 | 块数 | 中位块长 | **dense 自查 top1** | 重排后三类均值 |
|---|---|---|---|---|
| 递归1200（现状） | 6,711 | 1032 | **60%** | 0.633 |
| 纯段落1000 | 7,752 | 846 | **60%** | 0.631 |
| 递归600 | 14,766 | 464 | **69%** | 0.658 |
| **纯段落600** | **12,954** | 487 | **70%** | **0.670** |

**按结构切（纯段落1000）与定长切（递归1200）的 dense 自查完全相同（都 60%）；两个 ~500 字符的配置都是 69~70%。**
→ **"按结构切"本身没有额外收益，真正的杠杆是把块缩到 ~500 字符**（与实验17 ③ 的机制一致：长块稀释句子级语义）。

### ④ 300 行不行？——**不行，它在检索之前就切断了证据**

上面 ① 的对比只对齐了「交付信息量」(k×chunk=9600)，**没对齐「候选池信息量」**——`pool=100` 固定，于是 1200 的池装 120k 字符、300 的池只有 30k。补上这个控制变量（`pool×chunk≈120k`）后重测：

| 配置（池与交付都对齐） | 池覆盖 | 融合均值 | 重排均值 |
|---|---|---|---|
| 1200 / pool100 / k8 | 0.907 | 0.561 | 0.615 |
| **600 / pool200 / k16** | 0.883 | **0.631** | **0.656** |
| 300 / pool400 / k32 | **0.785** | 0.546 | 0.637 |

300 从 0.624 升到 0.637（池假设对了一部分），但**池覆盖反而最低（0.785）**——这不是池大小能解释的。根因是 **chunk 太小把证据句切断了**（gold 证据句均长 **157 字符**）：

| chunk | overlap | 块数 | **证据句完整留存** |
|---|---|---|---|
| 1200 | 150 | 6,711 | **100%** |
| **600** | 75 | 14,766 | **99%** |
| 300 | 37 | 32,040 | **89%** 🔴 |
| 300 | 150 | 33,752 | 97% |
| 300 | 200 | 34,823 | 97% |

**300 + overlap 37 切断 11% 的证据句**；把 overlap 加到 150/200 也只能救到 97%，代价是块数涨到 33,752（600 的 2.3 倍、1200 的 5 倍）。
**这是发生在检索之前的、不可恢复的损失**——证据句不完整存在于任何块中，检索再准也捞不到。300 唯一领先的 dense 自查 top1（79%），是在"没被切断的那 89%"上刷出来的。

> **工程规则（可复用）：`chunk_overlap` 必须 ≥ 要检索的最小语义单元长度。** 本项目是证据句 157 字符，而现行 600 配的 overlap 是 75——不够，丢了 1%。**建议 600 + overlap 150**，可补到 100%。

> **又一次"没控制住混杂变量"**：① 只对齐交付、忘了池；补上池之后才发现真凶是**证据句被切断**。**同一个错误在本实验内部又犯了一次**——控制变量要一路问到底："这个对比里还有什么在跟着变？"

### ⑤ 落地建议

**递归切分 chunk=600 + overlap=150 + pool=200 + k≈16**：证据句留存 ~100%、重排后三类均值 **0.656**（vs 现状 1200 的 0.615）。
若更看重索引体积，纯段落600 与递归600 精度持平且块数少 12%（见 ③）。

**对比现状**（递归1200/overlap150 + pool100 + k=16 ≈ 19200 字符上下文）→ **上下文减半（≈9600 字符），召回 0.615 → 0.656。**

> ⚠️ 未落地：改默认切分会让**实验1-17 的基线全部失效**（需重新编码 + 重跑），是否切换由用户定。
> ⚠️ 数值跨表勿比：实验17 ③ 报的 1200 自查是 **45%**、本实验 ③ 是 **60%**——取样不同（前者取语料前 60 条 fact，后者取抽中题目的 fact），**趋势一致（长块低、短块高），绝对值不可跨表比较**。

**方法论**：实验11 的 chunk 结论错，不是因为数据错，而是**对比没控制住"交付信息量"这个混杂变量**。这与实验15/16 的"聚合掩盖子群"、实验17 的"端到端指标看不见单点能力"合起来是同一类问题：**先问「这个对比里，除了我想变的那个变量，还有什么在变？」**

---

## 实验 19 · 推倒重来（一）：逐层重建 —— 修 embedding（chunk）与融合（pool）

**动机**（用户："既然证明 chunk 有问题，那就全部重来吧，这次得一步一个脚印了。既然 BM25 证明没问题，那 BM25 就先不动，先动 embedding，再动他们的融合，再动 reranker，每个流程都只用几条测试一下看看效果是否如预料般改进"）。

**方法**：新脚本 `eval_rebuild.py`，每层两条**缺一不可**的验收：

1. **单点自检**——给这一层**最有利的输入**，看它能否接近满分。embedding 层的最有利输入是**证据句的原文**（query 就是答案本身，没有任何语义鸿沟）；连这个都做不到 top1，就与"多跳难/问句刁钻"无关，是部件本身坏了。
2. **按题型均衡**——绝不看全类型聚合均值（实验15 的教训）。

> 实现注记：**故意不走 Chroma**，直接精确余弦，每个候选配置省掉 ~1.3G 磁盘；`.npy` 编码缓存与 `DenseRetriever` **同指纹**，所以选定配置后真正建库不重算。
> ⚠️ **两者并不完全等价**（`--layer 0` 实测，60 条 query）：**top1 一致率 100%**，但 **top100 的集合重合只有 96.9%(1200)/96.0%(600)**、完整名次相同仅 21.7%/16.7%——HNSW 的近似误差都落在 k 截断的边界上。故：自查类指标（看 top1）用探针无碍；**凡要推翻既有结论的对比，另用生产的 Chroma 复核一遍**（见实验20 ①）。
> 取样：每型 **30 题**，共 90 题 / **241 条 gold 证据句**。证据句长度：中位 **145**、均值 156、p90 235、最长 465 字符。

```bash
python eval_rebuild.py --layer 1 --n 30 --cfgs 1200x150,600x150,600x200,500x200,400x200
python eval_rebuild.py --layer 2 --n 30 --cfgs 1200x150,600x150,500x200 --pools 100,200,300
```

### ① 第1层 · 切分层：证据句还完不完整（**检索之前**就定死的上限）

| chunk/overlap | 片段数 | 全句留存 temporal / comparison / inference | 「前120字符」留存 |
|---|---|---|---|
| 1200 / 150（现状） | 6,711 | 98.7% / 100% / 100% | 100% |
| **600 / 150** | 15,172 | **100% / 100% / 100%** | 100% |
| 600 / 200 | 15,689 | 100% / 98.5% / 100% | 100% |
| 500 / 200 | 19,576 | 98.7% / 96.9% / 100% | 100% |
| 400 / 200 | 25,449 | **93.6% / 89.2% / 94.9%** 🔴 | 100% |

> 🔴 **一个新挖出来的度量盲区（本次重建最重要的方法论收获）**：**「前120字符」那一列全是 100%，而全句留存在 chunk=400 时已经掉到 89%。**
> 我们从实验10 起一直在用的 `fact@k` 只检查证据句的**前 120 字符**是否出现在交付文本里——**证据被切断它完全看不见**。
> 于是**块越小越占便宜**：小块把证据句拦腰截断，指标却照样给满分。所有跨 chunk 大小的 `fact@k` 比较都因此对小块系统性宽容。
> **规则升级**：控制变量不仅要控制输入（交付信息量、池信息量），还要检查**指标本身对被比较的变量是否中立**。

留存**不是单调**的（1200 的 temporal 98.7% 反而低于 600 的 100%）：递归切分的边界随 `chunk_size` 变化，某个句子在 1200 下被切开、在 600 下反而完整。所以"块越大越安全"也是错的。**600/150 是唯一三类全 100% 的配置。**

### ② 第1层 · embedding 单点自检：**用证据句原文当 query**，含它的片段排第几

| chunk/overlap | dense top1（t / c / i） | **dense top1 均值** | dense top10 | BM25 top1 均值 |
|---|---|---|---|---|
| 1200 / 150（现状） | 60% / 62% / 49% | **56.0%** | 83.8% | 97.9% |
| **600 / 150** | **76% / 72% / 71%** | **73.0%** | 92.9% | 98.8% |
| 600 / 200 | 78% / 74% / 67% | 72.6% | 93.4% | 98.8% |
| 500 / 200 | 82% / 77% / 77% | 78.4% | 91.7% | 100% |
| 400 / 200 | 83% / 78% / 85% | 82.6% | 94.6% | 97.5% |

- **dense top1 随块变小单调上升，且三个题型齐涨、没有子群被牺牲**（实验17 机制的确认：长块稀释句子级语义）。
- **BM25 全程 97~100% 不动** —— 既证实"BM25 是健康的那条腿、不用动"，也证明上面的变化来自 embedding 而非语料或取样。
- **overlap 150 → 200 在 chunk=600 上毫无收益**（73.0% vs 72.6%，留存都是 ~100%）：overlap ≥ 证据句中位长（145）就够了，再加只是白涨索引。

### ③ 第1层定档：**chunk=600 / overlap=150**

400 用 +9.6 个点的 dense top1 去换 **7~11% 的证据句被永久切断**，而这笔损失**我们的指标看不见**——不换。
500 的取舍居中（+5.4 点 top1、comparison 留存 96.9%），但它没有 600「三类全 100% 留存」的安全性。
**取 600/150：dense top1 56.0% → 73.0%（+17 点），证据句留存 100%，BM25 不动。**

### ④ 第2层 · 融合自检：融合**净增益 = 融合 − 更好的那条单路腿**（三类均值）

| chunk | pool | @4 | @8 | @16 | @32 |
|---|---|---|---|---|---|
| 1200 | 100 | **−0.026** ⛔ | +0.003 | +0.013 | +0.043 |
| 1200 | 200 | **−0.026** ⛔ | +0.003 | +0.018 | +0.043 |
| **600** | 100 | +0.022 | +0.034 | +0.051 | +0.044 |
| **600** | **200** | **+0.022** | **+0.034** | **+0.059** | **+0.046** |
| 500 | 200 | +0.041 | +0.009 | +0.033 | +0.053 |

**修好 embedding 之后，融合层换了性质**：chunk=1200 时它在浅层交付（@4）是**负功**、@8 约等于零；chunk=600 时**每个 k 都是正的**。
→ **实验16 的"融合从未提升交付"是坏 dense 腿的下游症状，不是 RRF 的性质。** 弱腿只往池里塞噪声，RRF 自然融不出东西；两条腿都健康时，盲区互补才真的发生。

### ⑤ 按**交付字符数**对齐的交付（融合后、未重排）

| 档位 | 配置 | temporal | comparison | inference | **均值** |
|---|---|---|---|---|---|
| 8,336 字符 | 1200 / pool100 / @8 | 0.439 | 0.650 | 0.517 | 0.535 |
| 8,336 字符 | **600 / pool200 / @16** | **0.522** | **0.794** | **0.561** | **0.626** |
| 16,672 字符 | 1200 / pool100 / @16 | 0.539 | 0.778 | 0.592 | 0.636 |
| 16,672 字符 | **600 / pool200 / @32** | **0.617** | **0.861** | **0.678** | **0.719** |

**同样多的上下文字符，600 比 1200 多交付 +0.091（8k 档）/ +0.083（16k 档）的 gold 证据，三个题型全部改善。**

### ⑥ 第2层定档：**pool=200**

chunk=600 下 pool 的边际收益（融合 fact@16 / 池覆盖，三类）：

| pool | 融合@16（t/c/i） | 池覆盖（t/c/i） |
|---|---|---|
| 100 | 0.522 / 0.778 / 0.553 | 0.822 / 0.933 / 0.817 |
| **200** | 0.522 / **0.794** / **0.561** | 0.867 / 0.944 / 0.869 |
| 300 | 0.522 / 0.794 / 0.561 | **0.889 / 0.978 / 0.967** |

**pool 200 → 300：池覆盖还在涨（inference 0.869→0.967），交付一位小数都没动。** 这是实验16 那条规律在**没有 reranker 参与**时的复现——多出来的候选排在融合列表的尾部，根本进不了 top-k。**扩大召回若不改变前排顺序，就不会变成交付。** 取 pool=200（与 `pool×chunk ≈ 恒定` 也一致：600×200 ≈ 1200×100）。

> **顺带查清了一个实现细节**：`_fuse_rrf` 最后一句是 `ranked[:self.pool]`（`retriever_hybrid.py`），而两条腿各召回 `pool` 个、并集最多 `2×pool`——**融合会丢掉最多一半的候选**。这就是上表里"融合的池覆盖偶尔低于单路腿"（⛔）的机制：不是 RRF 排错了，是**截断**把只被一条腿捞到的候选切掉了。它对交付无影响（那些候选本来也排在尾部），但**"融合后的池覆盖"这个数不能当作"两路召回的总覆盖"来读**。

### ⑦ 对既有结论的订正

- **实验16「融合从未提升交付、上游优化空间≈0」** → 在**修好 embedding 之后不再成立**（见 ④）。当时的测量没错，错在把"坏输入下的表现"当成了部件的固有属性。
- **实验18「~500 是甜点」** → 方向对（小块胜大块）、**幅度被高估**：那批 `fact@k` 用的是「前120字符」口径，对小块的截断视而不见（见 ①）。计入截断后落点是 **600 而非 500**。

**方法论**：这一轮的新教训是——**控制变量要控制到「指标本身」**。我们此前反复检查"输入端还有什么在跟着变"（实验18 的交付信息量、池信息量），但没检查过"**这把尺子对我要比的那个变量是不是中立的**"。`fact@k` 只看前 120 字符，恰恰对 chunk 大小不中立。

---

## 实验 20 · 推倒重来（二）：第3层 reranker —— **实验15 的头条结论没能复现**

**要回答的问题**：实验15 说 bge 在 temporal 上净减益 **−0.100**、51% 的证据 chunk 被往后推。那是在**坏掉的 embedding**（chunk=1200，dense 自查 top1 仅 56%）上量的。修好上游之后，这个负增益是**消失了**（说明它是坏输入的下游症状），还是**还在**（说明 reranker 与多跳目标不匹配，得换查询分解）？两种答案指向完全不同的下一步。

**方法**：`eval_rebuild.py --layer 3`，判据与第2层对称——**重排后的 fact@k 不该低于不重排**。
新增两样东西，正是这一轮的关键：**每型 60 题（实验15 是 15 题）** + **配对 bootstrap 95% 置信区间**。

```bash
python eval_rebuild.py --layer 3 --n 60 --cfgs 1200x150 --pool 100 --reranker bge --chroma   # 复现实验15 原配置
python eval_rebuild.py --layer 3 --n 60 --cfgs 600x150  --pool 200 --reranker bge            # 定档配置
```

### ① 先排除一个我自己引入的变量：**精确余弦探针 ≠ Chroma**

`eval_rebuild.py` 为省磁盘用精确余弦代替了 Chroma。要拿它的数去推翻旧实验，就得先证明这个替换不影响结论。`--layer 0` 实测（60 条 query）：**top1 一致 100%，但 top100 集合重合只有 96%、完整名次相同仅 17~22%。**
→ 于是把实验15 原配置**用生产的 Chroma 再跑一遍**（`--chroma`）。结果：

| | 探针 | Chroma |
|---|---|---|
| temporal Δ@8 | +0.031 [−0.042,+0.108] | **+0.031 [−0.042,+0.108]** |
| temporal 证据中位名次 | 8.0 → 7.0 | 8.0 → 7.0 |
| 重排后 fact@4/@8/@16 绝对值 | — | 与探针差 **≤0.005** |
| 融合后 fact 绝对值 | — | 与探针差 **≤0.017** |

**重排后的数字两者几乎完全相同，融合后才有差异**——说明 **reranker 把 HNSW 的近似误差吸收掉了**：那 4% 不一致的尾部候选，本来也不会被提上来。以下结论对两种实现都成立。

### ② 在**实验15 的原配置**上把每型 15 题加到 60 题（原 15 题是新样本的子集）

| 指标（chunk=1200, pool=100, bge） | 实验15（n=15） | 复现（n=60, Chroma） |
|---|---|---|
| temporal Δ@8 | **−0.100** 🔴 | **+0.031**，95% CI **[−0.042, +0.108]** |
| temporal 证据 chunk 被推后 | **51%** | 39%（被推前 43%） |
| temporal 证据中位名次 | 6 → **9**（推后） | 8 → **7**（推前） |

**n=60 的 95% 置信区间不包含 −0.100。** comparison / inference 每个 k 的区间都不跨 0（@8 分别 +0.144 [+0.083,+0.211]、+0.151 [+0.081,+0.226]），是真效应。

### ③ 在**定档配置**（chunk=600 / pool=200）上：temporal 依旧无可检出的效应

| type | Δ@4 [95%CI] | Δ@8 [95%CI] | Δ@16 [95%CI] | Δ@32 [95%CI] |
|---|---|---|---|---|
| **temporal** | +0.022 [−0.053,+0.100] | −0.039 [−0.111,+0.036] | −0.025 [−0.086,+0.039] | +0.028 [−0.033,+0.092] |
| comparison | +0.128 [+0.047,+0.208] ✅ | +0.133 [+0.069,+0.200] ✅ | +0.061 [+0.011,+0.114] ✅ | +0.036 [+0.000,+0.075] ✅ |
| inference | +0.118 [+0.047,+0.190] ✅ | +0.111 [+0.042,+0.183] ✅ | +0.094 [+0.026,+0.167] ✅ | +0.101 [+0.043,+0.160] ✅ |

**temporal 四个 k 的区间全部跨 0，且点估计正负横跳（+0.022 / −0.039 / −0.025 / +0.028）——这是噪声的形状，不是效应的形状。** 名次统计同样自相矛盾：中位名次 9→11（像被推后），可**被推前的比例（48%）反而高于被推后（43%）**。

### ④ 新旧配置在**等交付字符**下的正面对比（重排后，每型 60 题）

| 上下文预算 | 配置 | temporal | comparison | inference | **均值** |
|---|---|---|---|---|---|
| ~8.3k 字符 | 旧 1200 / pool100 / @8 | 0.503 | 0.797 | 0.651 | 0.650 |
| ~8.3k 字符 | **新 600 / pool200 / @16** | **0.522** | **0.822** | **0.671** | **0.672** |
| ~16.6k 字符 | 旧 1200 / pool100 / @16 | 0.603 | 0.858 | 0.785 | 0.749 |
| ~16.6k 字符 | **新 600 / pool200 / @32** | **0.661** | **0.878** | **0.797** | **0.779** |

**同样的上下文预算，新配置三个题型全部改善**（+0.030 均值 @16.6k，temporal 独得 +0.058）。
注意重排**缩小了**新旧差距（未重排时是 +0.059，重排后 +0.030）——cross-encoder 读长片段时有更多上下文可判断，长块的劣势被它补回了一部分。但**没补平**。

### ⑤ 结论与订正

1. **bge reranker 不是负资产**：comparison / inference 上每个 k 都是显著正增益（+0.04~+0.19），temporal 上**没有可检出的效应**（不是负的，也不是正的）。**保留 bge 作默认。**
2. **实验15 的 −0.100 是 n=15 的抽样噪声**（原文保留，已在该节加订正标注）。连带地：
   - 实验15 ③ 那张"temporal 天花板 = 不重排的 0.522"的表，其基线本身就是同一批 15 题的点估计；
   - **实验14「查询分解 temporal 0.578 是唯一越过天花板的做法」也建立在这个基线上（同样 n=15），因此其"唯一"的措辞不再有依据**——分解可能仍然有效，但**需要用 n≥60 + 置信区间重测才能下结论**（未做，成本是每题一次 LLM 调用）。
3. **第3层不需要换方案**。重建到此三层全部通过自检：BM25 未动、embedding 修好、融合转正、重排确认无害。

### ⑥ 方法论：**拆子群会让样本量变小，从而更容易把噪声读成信号**

实验15 得出的教训（"任何平均是正的都要按子群拆开验"）方向是对的，但它自己踩进了同一类陷阱的**另一面**：
把 45 题拆成 3 组各 15 题之后，每组的标准误差涨到约 0.09，而它据以下结论的效应量是 0.10——**信噪比约等于 1**。于是"分题型看"这个正确动作，产出了一个**错误的具体数字**，并被写成头条、进了 README、影响了后续三个实验的方向。

> **规则**：**分子群分析必须配置信区间**。拆得越细，越要问"这个数经得起重抽一次吗"。
> 本项目至此的四条度量教训连起来是同一件事——**先怀疑尺子，再怀疑被测对象**：
> 失败被吞成 0 分（实验12）→ 聚合掩盖子群（实验15）→ 端到端看不见部件半残（实验17）→ 对比没控制混杂变量（实验18）→ **指标本身对被比变量不中立（实验19）** → **子群样本太小把噪声读成信号（实验20）**。

---

## 实验 21 · 推倒重来（三）：端到端 A/B —— **检索侧的增益在 agentic 端到端上测不出来**

**方法**：同模型（`deepseek-v4-pro`）、同裁判、**同一批题**，唯一变量是整套检索配置。为此把配置做成 shell 级开关（`RAG_CHUNK_SIZE` / `RAG_CHUNK_OVERLAP` / `RAG_POOL` / `RAG_TOPK`），两臂各跑一遍、**逐题配对**：

```bash
RAG_CHUNK_SIZE=1200 RAG_POOL=100 RAG_TOPK=16 \
  python eval_agentic.py --per-type 30 --types comparison,inference,temporal \
                         --model deepseek-v4-pro --local ab_old.jsonl
python eval_agentic.py --per-type 30 ... --local ab_new.jsonl     # 新配置走默认值
```

### ① 先修两个**评测侧**的问题（否则量到的是评测自己的毛病）

1. **`_run_once` 会吞掉同轮的并行 tool 调用**。它在 `stream(stream_mode="values")` 的每个事件里只读 `messages[-1]`，而一轮里的多个 `rag_search` 会一次追加多条 ToolMessage。实测某题 **2 次检索只捕获 1 次**，`context_recall_fact` 被报成 **0.500 而真值是 1.000**。
   **这个 bug 只会让检索看起来更差**——"召回上不去"的表象里，有一部分是评测自己造成的。改成遍历**终态的完整消息列表**。
   ⚠️ 影响范围：`eval_agentic.py` 的历次实验（实验12 起）的 `context_recall_fact` / `groundedness` / `retrieval_relevance` 均**偏低**，幅度取决于当轮并行调用的频率。
2. **新增确定性指标 `n_search`**。此前"**模型压根没查**"与"**检索器没捞到**"在 `context_recall_fact` 上长得一模一样（都是 0.000）。实测 90 题里有 **6 题 agent 一次都没检索**——不区分开，归因会全错。**失败必须可见**（第 N 次栽在同一件事上）。
3. 顺带：`--local` 模式跳过 4 个 LLM 裁判与 LangSmith `evaluate()`，每题**便宜 5 倍**，且逐题落 JSONL 供配对。动机见 ③。

### ② 结果：**配对差异与 0 无法区分**（n=87，bootstrap 95% CI）

| 口径 | 旧 | 新 | **Δ(配对)** | 95% CI |
|---|---|---|---|---|
| 全部 87 题 | 0.581 | 0.603 | **+0.022** | [−0.066, +0.105] |
| 两臂都真检索过的 75 题 | 0.619 | 0.653 | **+0.034** | [−0.043, +0.110] |
| ↳ comparison (22) | 0.636 | 0.780 | **+0.144** | **[+0.008, +0.280]** ✅ |
| ↳ inference (27) | 0.571 | 0.512 | −0.059 | [−0.216, +0.093] |
| ↳ temporal (26) | 0.654 | 0.692 | +0.038 | [−0.045, +0.122] |

**只有 comparison 勉强显著，整体测不出差异。** 这与检索侧的测量（实验20 ④：等字符下 +0.030、三类齐涨、CI 收得很紧）**并不矛盾**，但需要解释。

### ③ 为什么检索侧量得到、端到端量不到

1. **agent 自己已经在做查询改写**。检索侧测量用的是**原始多跳问句**（元层面追问，与证据词汇不共享）；而 agent 实际发出的是 `"TechCrunch October 7 2023 Dave Clark Flexport"` 这种**事实层子问句**。chunk 缩小的收益主要来自"长块稀释句子级语义"，**问句越接近事实层，这个劣势本来就越小**——agent 的改写已经把一部分收益提前吃掉了。
2. **方差被 agent 的非确定性主导**。两臂看到的上下文不同 → 改写不同 → 检索次数不同（comparison 1.15 vs 1.44 次），配对设计**消不掉**这一项。
3. **功效算一下就知道做不到**：逐题配对差的标准差约 0.4，要把标准误压到 0.015（足以分辨 0.03）需要**每型约 700 题**。**这个基准 + 这个 agent，在任何可行的样本量下都分辨不了 0.03 的端到端效应。**

> **方法论：改动发生在哪一层，就在哪一层量。** 把检索侧的改动拿到端到端去验证，等于让它穿过 agent 改写、模型多跳意愿、裁判噪声三层放大器——信号没变大，噪声大了一个数量级。端到端该用来回答"这套系统整体行不行"，不该用来回答"我这个部件改好了没有"。

### ④ 这次改动到底买到了什么（诚实版）

- ✅ **检索侧确定的收益**（实验19-20，确定性、高功效）：dense 自查 top1 **56%→73%**；等字符交付 **0.749→0.779**（三类齐涨）；融合层从负功转为正贡献；证据句留存 100%。
- ✅ **成本侧确定的收益**：若 `k` 保持 16 不变，则**上下文减半**（8.3k vs 16.7k 字符）而召回仍略高（0.672 vs 0.650）。
- ⚠️ **端到端未能证实**：agentic 多跳下 **+0.022 [−0.066,+0.105]**，不显著。**不能宣称"端到端变好了"。**
- ⚠️ **代价**：索引从 6,711 涨到 15,172 片段（编码 ~3 分钟、磁盘 ~60MB/份）。

### ⑤ 环境限制（影响本轮可做的事，记录在案）

- **LangSmith 本月追踪配额已满**（`429 ... Monthly unique traces usage limit exceeded`）：数据集读写正常、`evaluate()` 能建实验，但 **run 追踪存不进去**。本实验的数字来自**本地聚合**，LangSmith 上没有可点开的 run。
- **网关只剩 14 个 deepseek 模型**，`grok-4.5` 再次下线（同实验13）。两臂都用 `deepseek-v4-pro`，A/B 内部可比；但**与实验1-12 的 grok 数字不可跨表比较**。
- deepseek 平均只检索 1.0~1.6 次（实验13 已记），**agentic 的收益本就被压制**，这也是端到端信号弱的原因之一。

---

## 实验 22 · 重测查询分解：**实验14 的两个前提都塌了，但方向一致、尚未证实**

**动机**：实验20 ⑤ 留下的唯一悬案。实验14 说"查询分解 + 每个子问句各自重排是**唯一**越过'不重排'天花板的做法（temporal 0.578 vs 0.522）"，可它的对照基线正是实验15 那批 **15 题**——而那批数据的 temporal 效应已被证明落在噪声内。**基线塌了，结论就不能再用。**

**方法**：升级 `eval_query.py`——加入 **「0 不重排」基线**（实验14 拿来当天花板的那条）、**分题型**、**相对 baseline 的配对 bootstrap 95% CI**、`k` 按新 chunk 对齐（8/16/32）、`--model` 参数。检索器/reranker 全程不变，唯一变量 = **用什么 query 去查**。

```bash
python eval_query.py --n 50 --types temporal,comparison,inference --model deepseek-v4-pro
```

### ① fact 级召回（chunk=600 / pool=200 / bge，每型 50 题）

| type | 策略 | @8 | @16 | @32 | 池覆盖 | **Δ@16 vs baseline [95%CI]** |
|---|---|---|---|---|---|---|
| **temporal** | 0 不重排 | 0.453 | 0.557 | 0.640 | 0.890 | +0.033 [−0.033,+0.100] |
| | 1 baseline | 0.417 | 0.523 | 0.660 | 0.890 | （基准） |
| | 2 multiquery | 0.397 | 0.503 | 0.647 | 0.877 | −0.020 [−0.060,+0.000] |
| | 3 decompose | 0.410 | 0.517 | 0.660 | 0.903 | −0.007 [−0.020,+0.000] |
| | **4 decompose-each** | 0.423 | **0.573** | **0.683** | 0.903 | **+0.050 [−0.017,+0.117]** |
| **comparison** | 0 不重排 | 0.640 | 0.767 | 0.847 | 0.947 | **−0.067 [−0.130,−0.007]** ⛔ |
| | 1 baseline | 0.783 | 0.833 | 0.883 | 0.947 | （基准） |
| | 2 multiquery | 0.753 | 0.823 | 0.867 | 0.957 | −0.010 [−0.060,+0.030] |
| | 3 decompose | 0.753 | 0.803 | 0.867 | 0.967 | −0.030 [−0.100,+0.020] |
| | 4 decompose-each | 0.713 | **0.840** | 0.863 | 0.967 | +0.007 [−0.067,+0.077] |
| **inference** | 0 不重排 | 0.440 | 0.555 | 0.685 | 0.883 | **−0.087 [−0.167,−0.007]** ⛔ |
| | 1 baseline | 0.555 | 0.642 | 0.770 | 0.883 | （基准） |
| | 2 multiquery | 0.565 | 0.652 | 0.768 | 0.935 | +0.010 [−0.018,+0.042] |
| | 3 decompose | 0.555 | 0.642 | 0.787 | 0.957 | +0.000 [−0.028,+0.032] |
| | **4 decompose-each** | 0.605 | **0.703** | **0.795** | 0.957 | **+0.062 [−0.017,+0.142]** |
| **全部 (150)** | 0 不重排 | 0.511 | 0.626 | 0.724 | 0.907 | −0.040 [−0.081,+0.001] |
| | 1 baseline | 0.585 | 0.666 | 0.771 | 0.907 | （基准） |
| | **4 decompose-each** | 0.581 | **0.706** | **0.781** | 0.942 | **+0.039 [−0.003,+0.082]** |

### ② 实验14 的两个前提，逐个检查

1. **前提 A：「不重排」是 temporal 的天花板** → **不成立**。n=50 下"不重排 − baseline"是 **+0.033，区间 [−0.033,+0.100] 跨 0**；而在 comparison / inference 上"不重排"是**显著更差**（−0.067 / −0.087，区间不跨 0）。**"不重排更好"是那 15 题特有的现象**，与实验20 的结论一致。
2. **前提 B：分解是「唯一」越过天花板的做法** → **措辞不成立**。decompose-each 在**任何一个题型上都没达到显著**（三个区间全跨 0）。

### ③ 但它仍是唯一值得继续投的方向

- **decompose-each 是唯一三类全为正的策略**（+0.050 / +0.007 / +0.062），整体 **+0.039 [−0.003,+0.082]** —— 下界只差 **0.003** 就不跨 0。
- **3 与 4 的对照复现了实验14 的机制主张**：两者**用完全相同的子问句、完全相同的候选池**（池覆盖一模一样：0.903 / 0.967 / 0.957），唯一差别是 top-k 名额怎么分配——**用原问句重排合并池（3）vs 每个子问句各自重排再拼（4）**。@16 上 4 比 3 高 **+0.057 / +0.037 / +0.061，三类同向**。
  → **"光换召回不够，重排也必须跟着子问句走"这个机制说法站得住；站不住的是它的效应量大小。**
- **又一次"覆盖涨了、交付不涨"**：分解把 inference 的池覆盖从 0.883 抬到 **0.957**（+0.074），multiquery 抬到 0.935，但 multiquery 的交付 **+0.010（跨 0）**。**扩大召回若不改变前排顺序，就不会变成交付**——这条规律在实验16、19 ⑥、22 上重复出现了三次。

### ④ 结论与下一步

- **不能说"查询分解有效"**，只能说：**它是唯一没有反效果、且三类同向为正的查询侧改动，值得用更大样本判定**。当前证据强度：整体 +0.039，区间下界 −0.003。
- **multiquery（同义改写）可以放弃了**：三类全无效应，只涨池覆盖。这与实验14 的定性结论一致，且这次带区间。
- 判定所需样本：把区间半宽从 0.042 压到 0.030 需 n 翻倍（每型 100 题）。已把 `eval_query.py` 的 LLM 改写阶段改成**并发**、并支持 `--dump` 落逐题 JSONL（供任意两两配对的事后分析，不必重跑），使 n=100/型 可行。

**方法论**：这次是**先修基线、再谈结论**的一个正面案例——如果不先发现实验15 的基线是噪声，就会继续在"越过 0.522 天花板"这个伪目标上做优化。**被推翻的不只是一个数字，还有它撑起来的整条方向判断。**

---

## 横向结论


1. **多跳把 correctness 拉起来**：三类可答题均值 correctness ≈ **0.80**（实验 5）vs 单次 **0.42**（实验 2）——方向性大赢（两次抽样非严格同批；严格受控见实验 4 的 context_recall 0.73 > 0.65）。
2. **代价：groundedness ~1.00（单次）→ ~0.73（多跳）**。多跳把多篇证据揉一起合成、断言更多 → 不如单次"贴着原文一句话"忠实。**收益与风险都被模型能力放大**（这条从项目开头到现在一以贯之）。
3. **context_recall 暴露了 rerank 的盲区**：相关性排序 ≠ 证据覆盖；`context_recall`(标题级)量得到覆盖缺口，但量不到"多跳把该跳的那块具体证据捞出来 + 在拼起来的上下文上推理"的增益——所以多跳 correctness 远高于 context_recall 的涨幅暗示的。
4. **该按题型选策略**：inference/comparison 上 agentic 值；temporal 是硬骨头（需更强时序/结构化检索）；null 上 agentic 已能诚实拒答（实验5 84% → 调优后实验7 96%）。
5. **别优化漏水的代理指标（实验6-9，Goodhart）**：title 级 `context_recall` 是代理；去重（实验8）把它刷到 0.89，却把 fact 级召回 0.645→0.530、correctness 拖低。**撤销去重、只留 k 4→8（实验9），correctness 反而最高 0.82、`context_recall` 诚实回到 0.80**。真正的赢是多交付**证据**（k），不是刷标题覆盖。**该盯 fact 级召回 / correctness，不是 title 级代理。** ~~检索覆盖已近顶（池含 92% 证据），后续靠 reranker 交付 + 生成推理。~~ ⚠️ 划掉这句：实验17-19 证明当时的 embedding 是半残的（自查 top1 仅 56%），"上游没空间了"的判断建立在坏部件之上。

6. **六条度量教训，是同一件事的六个面：先怀疑尺子，再怀疑被测对象。** 这份记录里**每一次方向性误判，都不是模型不行，而是量错了**：

   | # | 症状（当时读到的） | 真因 | 实验 |
   |---|---|---|---|
   | 1 | "模型变差了" | `except: return 空` 把网关 502 **吞成 0 分并计入均值** | 12 |
   | 2 | "重排在帮忙 +0.088" | **聚合掩盖子群**——得分题型看 | 15 |
   | 3 | "reranker 在 temporal 上净减益 −0.100" | **子群样本太小**（n=15，SE≈0.09 vs 效应 0.10），噪声被读成信号 | 20 |
   | 4 | "就这样了，上游没空间" | **端到端指标看不见部件半残**——缺单点自检 | 17 |
   | 5 | "1200 比 600 好" / "~500 是甜点" | **对比没控制混杂变量**（交付信息量、池信息量） | 18 |
   | 6 | "小块召回更高" | **指标本身对被比变量不中立**——`fact@k` 只查前 120 字符，看不见证据被切断 | 19 |
   | 7 | "召回上不去" | **评测吞掉并行 tool 调用**，把 1.000 报成 0.500 | 21 |

   由此固化成一套**每层验收的最小规程**（`eval_rebuild.py`）：
   **① 单点自检**（给该层最有利的输入，看能否接近满分）→ **② 按子群拆**（不看全类型均值）→ **③ 配对置信区间**（拆完必须问"这个数经得起重抽一次吗"）→ **④ 对齐混杂变量**（交付字符、池大小）→ **⑤ 检查尺子对被比变量是否中立**。
   缺任何一条，都会重复上表里的某一行。

7. **改动发生在哪一层，就在哪一层量**（实验21）。检索侧确定的 +0.030 增益，到 agentic 端到端上变成 **+0.022 [−0.066,+0.105]**——穿过 agent 改写、模型多跳意愿、裁判噪声三层放大器后，信号没变大、噪声大了一个数量级；要分辨它需每型约 700 题。**端到端该回答"系统整体行不行"，不该回答"我这个部件改好了没有"。**

> 一句话：**多跳不是万能钥匙,是"用忠实度和成本换覆盖与正确率"的一个可量化选择。** 每个结论都有同一套 LLM-judge + context_recall 的实测数字撑着，实验都在 LangSmith 数据集 `multihop-rag` 上可复现。
> 而这份记录真正的价值，可能不在于最后那套配置，而在于**上表那七行**——以及被推翻的结论**原文保留、只加订正标注**这件事本身。
